return {
	"mluders/comfy-line-numbers.nvim",
	config = function()
		require("comfy-line-numbers").setup({
			-- defaults are fine; you can tweak later
			-- labels, up_key, down_key, etc.
		})
	end,
}
